---
import VanderlandeImage from './graph.svg';
const { class: className } = Astro.props;
import { Image } from 'astro:assets';
---

<Image src={VanderlandeImage} class={(className || '') + " object-contain"} alt="Heatmap histogram"/>
