---
const { class: className } = Astro.props;
---

<div class={(className || '') + " bg-bg border-white/10 border rounded-xl p-4 shadow-2xl"}>
    <div class="flex gap-4">
        <div class="h-12 min-w-12 bg-purple-600 rounded"/>
        <div class="flex flex-col gap-3 flex-1">
            <div class="flex gap-2">
                <div class="h-2 w-24 bg-white/60 rounded mr-auto"/>
                <div class="h-2 w-2 bg-purple-600 rounded"/>
                <div class="h-2 w-2 bg-purple-600 rounded"/>
                <div class="h-2 w-2 bg-purple-600 rounded"/>
                <div class="h-2 w-2 bg-purple-600 rounded"/>
                <div class="h-2 w-2 bg-purple-600 rounded"/>
            </div>
            <div class="h-2 w-40 bg-white/60 rounded mr-auto"/>
            <div class="h-2 w-32 bg-white/60 rounded mr-auto"/>
        </div>
    </div>

    <div class="h-8 w-full bg-purple-600 mt-6 rounded"/>
</div>